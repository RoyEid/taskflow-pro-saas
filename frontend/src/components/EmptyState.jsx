/*
 * There were two near-identical empty states in the codebase, drifting
 * apart. This path is kept so existing imports keep working, but there is
 * now a single implementation in ui/EmptyState.
 */
export { default } from "./ui/EmptyState";
